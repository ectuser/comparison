// import 'reflect-metadata';

// import { Container } from 'inversify';
// import { TYPES } from '@product-comparison/container';
// import { ProductRepositoryPlugin, ProductUseCase, SelectedProductsRepositoryPlugin } from '@product-comparison/product-core';
// import { ProductState } from '@product-comparison/product-state';
// import { ProductRepository } from '@product-comparison/api';

// const myContainer = new Container();

// myContainer.bind<ProductUseCase>(TYPES.ProductUseCase).to(ProductUseCase);
// myContainer.bind<SelectedProductsRepositoryPlugin>(TYPES.SelectedProductsRepositoryPlugin).to(ProductState);
// myContainer.bind<ProductRepositoryPlugin>(TYPES.ProductRepositoryPlugin).to(ProductRepository);

// export { myContainer };
